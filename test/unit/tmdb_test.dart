import 'package:flutter_test/flutter_test.dart';

// import '../../lib/model/movie.dart';
// import '../../lib/data/network/tmdb.dart';

main() async {
  group('Tmdb tests', () {
    // final query = {
    //   "page": 1,
    // };

    test('Fetch movies successfully', () async {
      // List<Movie> movies = await Tmdb().getMovies(query: query);

      expect(20, 20);
    });
  });
}
